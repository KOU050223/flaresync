{
  "name": "flaresync-example-basic",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "wrangler:dryrun": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "flaresync": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^6.0.0",
    "wrangler": "^4.84.0"
  }
}
